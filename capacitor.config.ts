import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'semestralni_projekt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
